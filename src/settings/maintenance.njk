---
layout: base.njk
title: Maintenance
scripts: '<script type="module" src="/js/settings/maintenance.js"></script>'
---
<!-- Sub-navigation -->
<div class="bg-white border-b border-gray-200 sticky top-14 z-30">
  <div class="max-w-6xl mx-auto px-4 py-2 flex items-center justify-between min-h-[52px]">
    <nav aria-label="Breadcrumb">
      <ol class="flex items-center text-sm">
        <li class="flex items-center min-w-0">
          <a href="/" class="text-gray-500 hover:text-primary hover:underline">Home</a>
        </li>
        <li class="flex items-center min-w-0">
          <i data-lucide="chevron-right" class="w-4 h-4 text-gray-400 mx-1 flex-shrink-0" aria-hidden="true"></i>
          <a href="/settings/" class="text-gray-500 hover:text-primary hover:underline">Settings</a>
        </li>
        <li class="flex items-center min-w-0">
          <i data-lucide="chevron-right" class="w-4 h-4 text-gray-400 mx-1 flex-shrink-0" aria-hidden="true"></i>
          <span class="text-gray-900 font-medium" aria-current="page">Maintenance</span>
        </li>
      </ol>
    </nav>
  </div>
</div>

{% set activePage = 'maintenance' %}
{% include "settings-nav.njk" %}

<div class="max-w-2xl mx-auto px-4 py-6 page-content">
  <h1 class="text-2xl font-bold text-gray-900 mb-6">Maintenance</h1>

  <!-- Library Health -->
  <div class="bg-white rounded-xl border border-gray-200 p-6 mb-4">
    <h3 class="font-medium text-gray-900 mb-2">Library Health</h3>
    <p class="text-gray-600 text-sm mb-4">Analyse your library for missing data and fix issues from book APIs.</p>

    <!-- Loading State -->
    <div id="health-loading" class="space-y-3">
      <div class="skeleton h-6 w-48 rounded"></div>
      <div class="skeleton h-4 w-full rounded"></div>
      <div class="skeleton h-4 w-3/4 rounded"></div>
    </div>

    <!-- Health Summary -->
    <div id="health-summary" class="hidden">
      <!-- Progress Bar -->
      <div class="mb-4">
        <div class="flex items-center justify-between mb-1">
          <span class="text-sm font-medium text-gray-700">Library Completeness</span>
          <span id="health-score" class="text-sm font-medium">--%</span>
        </div>
        <div class="w-full bg-gray-200 rounded-full h-3">
          <div id="health-progress-bar" class="h-3 rounded-full transition-all" style="width: 0%"></div>
        </div>
        <p id="health-rating" class="text-sm text-gray-500 mt-1"></p>
      </div>

      <!-- Stats Row -->
      <div class="flex flex-wrap gap-4 mb-4 text-sm">
        <div class="text-gray-600">
          <span class="font-medium" id="health-total-books">0</span> books
        </div>
        <div class="text-gray-600">
          <span class="font-medium" id="health-issues-count">0</span> books with missing information
        </div>
      </div>

      <!-- Issues List (expandable sections) -->
      <div id="health-issues" class="space-y-2 mb-4">
        <!-- Issue rows will be rendered here by JS -->
      </div>

      <!-- Empty State -->
      <div id="health-complete" class="hidden p-4 bg-green-50 border border-green-200 rounded-lg text-center">
        <i data-lucide="check-circle" class="w-8 h-8 text-green-500 mx-auto mb-2" aria-hidden="true"></i>
        <p class="text-green-700 font-medium">Your library is 100% complete!</p>
        <p class="text-green-600 text-sm">All books have cover images, genres, and metadata.</p>
      </div>

      <!-- Action Buttons -->
      <div id="health-actions" class="flex flex-wrap gap-2">
        <button id="health-refresh-btn" class="flex items-center gap-2 px-4 py-2 bg-gray-200 hover:bg-gray-300 text-gray-700 rounded-lg transition-colors min-h-[44px]">
          <i data-lucide="refresh-cw" class="w-4 h-4" aria-hidden="true"></i>
          <span>Refresh</span>
        </button>
      </div>
    </div>
  </div>

  <!-- Recalculate Genre Counts -->
  <div class="bg-white rounded-xl border border-gray-200 p-6 mb-4">
    <h3 class="font-medium text-gray-900 mb-2">Recalculate Genre Counts</h3>
    <p class="text-gray-600 text-sm mb-4">If genre book counts appear incorrect, this will scan all books and recalculate the count for each genre.</p>
    <div id="recount-results" class="hidden mb-4 p-4 bg-gray-50 rounded-lg border border-gray-200">
      <p id="recount-results-text" class="text-sm text-gray-700"></p>
    </div>
    <button id="recount-genres-btn" class="flex items-center gap-2 px-4 py-2 bg-amber-500 hover:bg-amber-600 text-white rounded-lg transition-colors min-h-[44px]">
      <i data-lucide="calculator" class="w-4 h-4" aria-hidden="true"></i>
      <span>Recalculate Counts</span>
    </button>
  </div>

  <!-- Orphaned Images -->
  <div class="bg-white rounded-xl border border-gray-200 p-6 mb-4">
    <h3 class="font-medium text-gray-900 mb-2">Orphaned Images</h3>
    <p class="text-gray-600 text-sm mb-4">Find and delete images in storage that are not linked to any book. This can happen if you upload images but don't save the book.</p>

    <!-- Scan Results -->
    <div id="orphan-results" class="hidden mb-4 p-4 bg-gray-50 rounded-lg border border-gray-200">
      <div id="orphan-loading" class="hidden flex items-center gap-2">
        <div class="w-4 h-4 border-2 border-amber-500 border-t-transparent rounded-full animate-spin"></div>
        <span class="text-sm text-gray-600">Scanning for orphaned images...</span>
      </div>
      <div id="orphan-found" class="hidden">
        <div class="flex items-center gap-2 text-amber-600 mb-2">
          <i data-lucide="alert-triangle" class="w-5 h-5" aria-hidden="true"></i>
          <span class="font-medium"><span id="orphan-count">0</span> orphaned images found</span>
        </div>
        <p class="text-sm text-gray-600 mb-3">Total size: <span id="orphan-size">0 KB</span></p>
        <button id="delete-orphans-btn" class="flex items-center gap-2 px-4 py-2 bg-red-600 hover:bg-red-700 text-white rounded-lg transition-colors min-h-[44px]">
          <i data-lucide="trash-2" class="w-4 h-4" aria-hidden="true"></i>
          <span>Delete Orphaned Images</span>
        </button>
      </div>
      <p id="orphan-none" class="hidden text-sm text-gray-700">No orphaned images found.</p>
      <p id="orphan-deleted" class="hidden text-sm text-gray-700"><span id="orphan-deleted-count">0</span> orphaned images deleted.</p>
    </div>

    <button id="scan-orphans-btn" class="flex items-center gap-2 px-4 py-2 bg-amber-500 hover:bg-amber-600 text-white rounded-lg transition-colors min-h-[44px]">
      <i data-lucide="search" class="w-4 h-4" aria-hidden="true"></i>
      <span>Scan for Orphaned Images</span>
    </button>
  </div>

</div>

<!-- Toast -->
<div id="toast" class="hidden fixed bottom-6 left-4 right-4 sm:left-auto sm:right-4 sm:w-80 bg-gray-800 text-white px-4 py-3 rounded-lg shadow-lg z-50"></div>
